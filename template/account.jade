extends layout

block content
    p
        | You are logged in as #{email}
    p
        | Your full name is #{user.displayName}

block actionbuttons
    a(class='btn btn-primary', href='/logout') Logout