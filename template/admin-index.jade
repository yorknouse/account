extends admin-layout

block content
    h2 Admin Menu
    noscript
        div(class='alert alert-danger', role='alert')
            p You need to enable Javascript to use the Admin section
    div(class='btn-group-vertical half-width central override-display')
        a(role='button', class='btn btn-primary', href='/admin/users') Users
        a(role='button', class='btn btn-primary', href='/admin/sessions') Sessions
        a(role='button', class='btn btn-primary', href='/admin/api') API
        a(role='button', class='btn btn-primary', href='/admin/content') Content
        a(role='button', class='btn btn-primary', href='/admin/report') Reports